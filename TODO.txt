finish customizing:
- wlogout
  - TODO: the icons are slightly blurrier than I would like
- plymouth
- sddm
- hyprshutdown
  - make the background the wallpaper or the catppuccin base color (avoid animations while stuff is closing)

make the default.nix files autoscan dirs
fix the git repo for nix-config not updating properly because of perms issues
customize sudo
make wofi emoji say "Search emojis" instead of "emoji"

make grimblast:
- include window borders
- incude corners of windows
- not include the outside of the windows
- not have an annoying flicker when first starting up
- redo the visuals

patch hyprland to change the default gray to catppuccin color
consider seeing if i can change the hyprland startup animation
fix borders in fullscreen windows appearing (such as games)
fix certain apps not being shown as floating
fix window decorations appearing

patch waybar to fix issues i have with it:
- no animations
- dodgy animations
- curosr not being a pointer with the hyprland indicator
- shadows being clipped
- notifications being handled in such a dodgy way (checking every second)
- ensure that the nixos icon and notification icon are perfect circles
- fix the hitboxed of rounded elements
- custom order for tray elements
- custom icons for tray elements
- better ways to handle settings icons
- waybar itself should have better tooltip support and in general, better control for the contents of elements

patch wlogout
- [X] make hovering over an option select it
- [X] fix the mouse pointer not being set
- [X] fix the issue with the arrow keys working while something is hovered
- [ ] ensure shadow clipping isn't occuring
- [ ] fix the hitboxed of rounded elements
- [ ] make it immediately die after suspending so it doesn't show up after hyprlock turns back on
  - [ ] might just be an issue with hyprlock

themeing:
      - get apple emojis
    install catppuccin gtk and qt themes
    install catppuccin themes for all apps
    disable all window buttons in apps
- FIX APP SCALING

cursor:
- add shadow to the cursor by patching it
- change the catppuccin loading + waiting cursor to an infinity symbol
- change the border color of the cursor to mauve

ALTERNATIVE APPS:
- codium
- nemo
- calculator -> integrate into app launcher
- Ark replacement (archive manager)
- Okular -> Zen
- Gwenview -> TODO
- TODO: music player
- TODO: video player

List of sounds to make:
- notification
- authentication required
- login
- logout
- shutdown
- wifi connected
- wifi disconnected
- volume up
- volume down
- drive connected
- drive disconnected
- drive failed
- low battery
- critical battery
