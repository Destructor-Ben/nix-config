# NEXT:
- fix hyprshutdown not working when run from waybar
  - consider running a background daemon that will launch wlogout using IPC, and waybar just informs wlogout to launch
- fix plymouth
- disable second screen in sddm
- disable time compat for windows (i never boot into windows anyway)
- get critical battery notifiactions working
- see if there is a way to make each monitor have their "selected" but not "focused" workspace have a css class

# NEXT:
- get formatter set up for flake
- get the dir autoreading working for default.nix

TODO: move codium settings.json + extensions to nixos settings
TODO: move zen settings/user chrome css to nixos settings

customize zen and codium themes because both kinda suck

TODO: finish configuring hyprland settings + animations

finish customizing:
- plymouth
- sddm
- hyprshutdown

TODO: change to 24 hour time

fix the git repo for nix-config not updating properly because of perms issues
customize sudo

TODO: fix wofi emoji from typing text when selecting an emoji

make grimblast:
- include window borders
- incude corners of windows
- not include the outside of the windows
- not have an annoying flicker when first starting up
- redo the visuals

patch waybar to fix issues i have with it:
- no animations
- dodgy animations
- curosr not being a pointer with the hyprland workspace indicator or the system tray
- shadows being clipped
- notifications being handled in such a dodgy way (checking every second)
- ensure that the nixos icon and notification icon are perfect circles
- fix the hitboxed of rounded elements
- custom order for tray elements
- custom icons for tray elements
- better ways to handle settings icons
- waybar itself should have better tooltip support and in general, better control for the contents of elements
- workspaces
  - make each monitor store the current workspace even if it isn't active (maybe this is already done), so it can at least have a shadow bg col

patch wofi
- [ ] make clicking outside of wofi close it
- [ ] make an "Exit" option selected by default instead of the top option

themeing:
    - get apple emojis
    install catppuccin gtk and qt themes
    install catppuccin themes for all apps
    theme spotify
    theme obsidian

cursor:
- add shadow to the cursor by patching it
- change the catppuccin loading + waiting cursor to an infinity symbol
- change the border color of the cursor to mauve

List of sounds to make:
- notification
- authentication required
- login
- logout
- shutdown
- wifi connected
- wifi disconnected
- volume up
- volume down
- drive connected
- drive disconnected
- drive failed
- low battery
- critical battery
